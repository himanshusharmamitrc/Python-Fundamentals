#use the sort() method to sort the list in ascending order.
fruits = ["cherry", "apple", "banana"]
fruits.sort()
print(fruits)

#use the sort() method to sort the list in descending order.
numbers = [4, 2, 1, 3]
numbers.sort(reverse=True)
print(numbers)
